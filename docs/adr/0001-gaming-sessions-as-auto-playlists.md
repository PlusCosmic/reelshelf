# Gaming Sessions as Auto-Generated Playlists

Gaming Sessions are represented as auto-generated playlists keyed by owner, game category, and plain gaming-day date. The playlist remains editable as a user-facing collection, but its session identity is durable and independent of the display name, so future uploads for the same game/day can merge into the same session through a backend endpoint instead of fragile playlist-name matching.
