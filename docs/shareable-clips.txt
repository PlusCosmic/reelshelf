1. Button on video to share clip via link
1.1 Stretch goal to share with discord friend(s)/server with a populated list
2. Video is marked as shared and creates a new link that directs to a stripped down video player page that is accessible for unauthenticated users
