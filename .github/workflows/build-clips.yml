name: Build Clips App

on:
  pull_request:
    branches: [develop, main]
    paths:
      - 'apps/clips/**'
      - 'packages/shared/**'
      - 'packages/ui/**'
      - 'packages/nucleus-api-client/**'
      - 'packages/typescript-config/**'
      - 'pnpm-lock.yaml'
      - '.github/workflows/build-clips.yml'

jobs:
  build:
    name: Build & Typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build clips app
        run: pnpm turbo run build --filter=clips

      - name: Typecheck
        run: pnpm --filter clips typecheck
