[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]
cacheDirectories = ["node_modules", ".pnpm-store"]

[phases.build]
cmds = ["pnpm --filter home build"]

[staticAssets]
directory = "dist"
